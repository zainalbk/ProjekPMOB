<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Top Gradient Background -->
    <View
        android:id="@+id/topGradientBg"
        android:layout_width="0dp"
        android:layout_height="138dp"
        android:background="@drawable/bg_gradient_top_iot"
        android:elevation="6dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Status Bar Placeholder -->
    <View
        android:id="@+id/statusBarSpace"
        android:layout_width="0dp"
        android:layout_height="54dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Profile Section -->
    <LinearLayout
        android:id="@+id/profileSection"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:layout_marginTop="19dp"
        app:layout_constraintTop_toBottomOf="@id/statusBarSpace"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Profile Picture -->
        <ImageView
            android:id="@+id/ivProfile"
            android:layout_width="53dp"
            android:layout_height="53dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_profile_placeholder"
            tools:src="@tools:sample/avatars" />

        <!-- Welcome Text -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Welcome Back,"
                android:textColor="#240072"
                android:textSize="12sp"
                android:lineSpacingMultiplier="0.99" />

            <TextView
                android:id="@+id/tvUsername"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="your name !"
                android:textColor="#240072"
                android:textSize="12sp"
                android:lineSpacingMultiplier="0.99" />
        </LinearLayout>

        <!-- Notification Icon with Badge -->
        <FrameLayout
            android:id="@+id/btnNotification"
            android:layout_width="29dp"
            android:layout_height="29dp"
            android:background="?attr/selectableItemBackgroundBorderless">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_notifications"
                android:background="@drawable/bg_circle_teal_filled"
                android:padding="6dp"
                app:tint="@android:color/white" />

            <!-- Red Badge -->
            <View
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_gravity="end|top"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="2dp"
                android:background="@drawable/dot_red_8" />
        </FrameLayout>
    </LinearLayout>

    <!-- Main Content -->
    <ScrollView
        android:id="@+id/scrollContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/profileSection"
        app:layout_constraintBottom_toTopOf="@id/bottomNav"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="28dp"
            android:paddingStart="7dp"
            android:paddingEnd="7dp">

            <!-- Device Card -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardDevice"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="13dp"
                android:layout_marginEnd="13dp"
                android:layout_marginTop="15dp"
                app:cardCornerRadius="11dp"
                app:cardElevation="3dp"
                app:strokeColor="#99F7FF"
                app:strokeWidth="1dp"
                app:cardBackgroundColor="#EEFEFF">

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="61dp"
                    android:padding="11dp">

                    <!-- Device Icon -->
                    <View
                        android:id="@+id/deviceIconBg"
                        android:layout_width="38dp"
                        android:layout_height="39dp"
                        android:background="@drawable/bg_rounded_6"
                        app:layout_constraintStart_toStartOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent" />

                    <ImageView
                        android:id="@+id/ivDeviceIcon"
                        android:layout_width="26dp"
                        android:layout_height="26dp"
                        android:src="@drawable/ic_machine_foreground"
                        app:tint="@android:color/white"
                        app:layout_constraintStart_toStartOf="@id/deviceIconBg"
                        app:layout_constraintEnd_toEndOf="@id/deviceIconBg"
                        app:layout_constraintTop_toTopOf="@id/deviceIconBg"
                        app:layout_constraintBottom_toBottomOf="@id/deviceIconBg" />

                    <!-- Device Info -->
                    <LinearLayout
                        android:id="@+id/deviceInfo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:layout_marginStart="15dp"
                        app:layout_constraintStart_toEndOf="@id/deviceIconBg"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent">

                        <TextView
                            android:id="@+id/tvDeviceName"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="Feeder 1"
                            android:textColor="#000000"
                            android:textSize="15sp"
                            android:lineSpacingMultiplier="1.29" />

                        <TextView
                            android:id="@+id/tvDeviceId"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="#12544368"
                            android:textColor="#5B5B5B"
                            android:textSize="8.5sp"
                            android:lineSpacingMultiplier="1.29"
                            android:layout_marginTop="2dp" />
                    </LinearLayout>

                    <!-- Toggle Switch -->
                    <androidx.appcompat.widget.SwitchCompat
                        android:id="@+id/switchDevice"
                        android:layout_width="40dp"
                        android:layout_height="20dp"
                        android:checked="false"
                        android:thumb="@drawable/switch_thumb_custom"
                        android:track="@drawable/switch_track_custom"
                        app:layout_constraintEnd_toEndOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        app:layout_constraintBottom_toBottomOf="parent" />
                </androidx.constraintlayout.widget.ConstraintLayout>
            </androidx.cardview.widget.CardView>

        </LinearLayout>
    </ScrollView>

    <!-- Bottom Navigation -->
    <LinearLayout
        android:id="@+id/bottomNav"
        android:layout_width="0dp"
        android:layout_height="130dp"
        android:orientation="vertical"
        android:gravity="bottom"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Wave Background -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="114dp">

            <!-- Bottom Nav Background -->
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/bg_bottom_nav_wave"
                android:scaleType="fitXY"
                android:layout_marginTop="16dp" />

            <!-- Nav Items Container -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:gravity="center"
                android:paddingTop="16dp">

                <!-- Machine Icon (Left) -->
                <LinearLayout
                    android:id="@+id/btnMachineNav"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:focusable="true">

                    <FrameLayout
                        android:layout_width="62dp"
                        android:layout_height="62dp"
                        android:background="@drawable/bg_avatar_circle"
                        android:backgroundTint="#99F7FF">

                        <ImageView
                            android:layout_width="29dp"
                            android:layout_height="match_parent"
                            android:layout_gravity="center"
                            android:src="@drawable/ic_machine_foreground"
                            app:tint="#004D40" />
                    </FrameLayout>
                </LinearLayout>

                <!-- Home Icon (Center) -->
                <LinearLayout
                    android:id="@+id/btnHomeNav"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:focusable="true">

                    <ImageView
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:src="@drawable/ic_home"
                        android:padding="8dp"
                        app:tint="#004D40" />
                </LinearLayout>

                <!-- Profile Icon (Right) -->
                <LinearLayout
                    android:id="@+id/btnProfileNav"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:clickable="true"
                    android:focusable="true">

                    <ImageView
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:src="@drawable/ic_person"
                        android:padding="8dp"
                        app:tint="#004D40" />
                </LinearLayout>
            </LinearLayout>
        </FrameLayout>
    </LinearLayout>

    <!-- Gradient Overlay (Optional - for aesthetic) -->
    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/bg_gradient_overlay"
        android:alpha="0.35"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>